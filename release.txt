Deploy-hygiene + auto-merge fixes; everything since v1.21.2.

Highlights:
- `setup install` now performs a complete standalone manual install
  (services, /service symlinks, rc.local boot block) - replaces the
  removed legacy install.sh; recipe: wget tarball, `setup install auto`
- Auto-merge workflow fixed (label mismatch + retry window): PRs labeled
  `auto-merge` actually merge themselves after CI
- README install docs match reality

Full list: CHANGELOG.md [1.21.3]
